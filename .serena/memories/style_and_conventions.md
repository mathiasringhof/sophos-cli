Follow repository guidance from `AGENTS.md`: PEP 8, 4-space indentation, `snake_case` for functions and variables, `PascalCase` for classes, and type hints on public functions. Keep `cli.py` lightweight, push reusable API logic into services or shared helpers, and let Typer handlers mainly validate input, construct models, call services, and render results. Tests should prefer mocks or in-memory fakes over live firewall calls.
