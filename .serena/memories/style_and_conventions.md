Follow repository guidance from AGENTS.md: keep cli.py lightweight, move reusable API logic into services, use snake_case for functions/variables and PascalCase for classes, prefer type hints on public functions, and keep functions focused. Typer command handlers should mainly collect input, construct models, call services, and render results. Tests prefer mocks/fakes over live firewall calls except explicit integration coverage.