Key commands: `uv sync`, `uv run sophos-cli --help`, `uv run python -m sophos_cli --help`, `uv run pytest`, `uv run ruff check .`, `just analyze`, `just format`, `just test`, and `just test-cov`. Useful CLI examples now include `uv run sophos-cli test-connection`, `uv run sophos-cli dns list`, `uv run sophos-cli network ip-host create branch-office --ip-address 192.0.2.44`, and `uv run sophos-cli api get-ip-host --name branch-office`.
