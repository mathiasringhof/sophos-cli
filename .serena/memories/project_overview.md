sophos-cli is a Python CLI for Sophos Firewall automation built with Typer and Rich. Code follows a src/ layout under src/sophos_cli with lightweight CLI handlers, domain command groups in commands/, reusable logic in services/, Pydantic models in models/, and tests in tests/. The repo currently uses the sophosfirewall-python SDK and also supports raw XML-style access via generic commands.